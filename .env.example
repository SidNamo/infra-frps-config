# ======================
# 🧩 FRPS (서버 설정)
# ======================

# FRP 서버가 클라이언트 연결을 수신할 포트
# FRPS_BIND_PORT=7000

# FRP REST API (동적 frpc 등록용)
# FRPS_API_PORT=7400

# FRP 대시보드 (관리자용 웹 UI)
# FRPS_DASH_PORT=7500

# 대시보드 로그인 계정
FRPS_DASH_USER=admin
FRPS_DASH_PWD=admin123

# 클라이언트 등록 및 API 보안을 위한 토큰
FRPS_TOKEN=changeme-secret

# 로그 레벨 (trace, debug, info, warn, error)
LOG_LEVEL=info


# ======================
# ☁️ KEEPALIVE (슬립 방지용)
# ======================

# Render 서비스 도메인 주소
# ex) DOMAIN=my-frps-service.onrender.com
DOMAIN=your-render-app.onrender.com
